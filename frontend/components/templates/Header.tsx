import { VFC } from "react";
const Header: VFC = () => {
  return (
    <div className="w-full px-10">
      <span className="text-[40px] font-bold text-[#ff4500] cursor-pointer">
        Sentencify
      </span>
    </div>
  );
};

export default Header;
